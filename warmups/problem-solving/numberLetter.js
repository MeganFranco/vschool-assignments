//Write a JavaScript function to  get the number of occurrences of each letter in a string

function getNumber(string){
    var letters = string.split('')
    var countLetters = [["a", 1], ["b", 4]]
    
    for(var i = 0; i < letters.length; i++){
        
        for(var j = 0; j < countLetters.length; j++){
            if(countLetters[j][0])
        }
    }
}